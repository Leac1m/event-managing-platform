services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./sqlite_data:/app/data
      - ./uploads:/app/uploads
    environment:
      - PORT=3001
      - DATABASE_URL=/app/data/sqlite.db
      - PROFILE_UPLOAD_DIR=/app/uploads
      - JWT_SECRET=change-this-in-production
